{
  "name": "tweet2md",
  "version": "1.1.0",
  "description": "Copy or Download Twitter/X Threads and Articles as Markdown",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "npm run build && cd dist && zip -r ../tweet2md.zip . && echo '📦 tweet2md.zip created'",
    "clean": "rm -rf dist tweet2md.zip"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "@types/chrome": "^0.0.287",
    "@types/turndown": "^5.0.5"
  },
  "dependencies": {
    "turndown": "^7.2.0"
  }
}
