{
  "name": "tweet2md",
  "version": "1.8.0",
  "description": "One-click export of X (Twitter) threads, tweets, and articles to clean Markdown with images and metadata. Perfect for Obsidian, Notion, and AI/RAG workflows. Works locally—no API, no tracking.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "npm run build && cd dist && zip -r ../tweet2md.zip . && echo '📦 tweet2md.zip created'",
    "clean": "rm -rf dist tweet2md.zip",
    "test": "vitest run",
    "test:watch": "vitest",
    "screenshots": "npm run build && node scripts/popup-screenshots.mjs"
  },
  "devDependencies": {
    "@puppeteer/browsers": "^3.0.4",
    "@types/chrome": "^0.1.42",
    "@types/jsdom": "^28.0.3",
    "esbuild": "^0.28.0",
    "jsdom": "^29.1.1",
    "puppeteer-core": "^25.1.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "jspdf": "^4.2.1"
  }
}
